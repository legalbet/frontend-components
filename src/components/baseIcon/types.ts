import type { IconNames } from '@/components/baseIcon/iconNames';

export interface IconParams {
  tagName?: string;
  size?: string;
  iconName: IconNames;
  textColor?: string;
  fontSize?: string;
  title?: string;
  pointed?: boolean;
  weight?: string;
}
